# 可选：强制浏览器直连 Collector（会走跨域；本地开发建议留空，用下方 COLLECTOR_PROXY_TARGET + 默认 /api/collector）。
# NEXT_PUBLIC_COLLECTOR_URL=http://127.0.0.1:8787

# Next.js /api/collector/* 代理到真实 Collector（仅服务端使用，默认 8787）
COLLECTOR_PROXY_TARGET=http://127.0.0.1:8787
